let config = {};

config.envoriment = 'PRODUCTION';
config.port = 3050;

config.db = {
    host: 'localhost',
    user: 'root',
    port: 3306,
    password: null,
    database: 'exampleAPI'
};

module.exports = config;